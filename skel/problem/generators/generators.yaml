#solution: /submissions/accepted/submission.py
#visualizer: /visualizers/asy.sh {name}
# By default, generated files are not ignored.
#gitignore_generated: false

data:
  sample:
    type: directory
    # Uncomment to disable generating ans files for sample cases.
    #solution:
    data:
      #'1':

  secret:
    type: directory
    data:
      # Named testcases:
      # random: random_generator.py large {seed}
      # Numbered testcases:
      # - random: random_generator.py large {seed}
