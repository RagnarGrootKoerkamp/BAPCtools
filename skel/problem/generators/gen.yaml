config:
    # Generate a .ans for each .in that's found.
    generate_ans: true
    # Could also live in generators/.
    submission: # submissions/accepted/solution.cpp

secret:
    # test.in: generator.py $SEED
